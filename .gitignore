# 控えに入れないもの。tools/sync-from-website.ps1 が写さないもの・tools/push-github.ps1 が弾くものと同じ。

# 秘密(.env.example は入れる)
.env
!.env.example
*.local.php
*.pem
*.key
*.jks
*.keystore
*.p12
*.pfx
id_rsa
id_ed25519

# 利用者のデータ・控え
server/src/uploads/
server/backups/
*.cms
*.tar.gz

# 生成物・退避(src/vendor は composer install で戻る。admin/vendor と vendor-web は手で置いたものなので入れる)
server/src/vendor/
server/Downloaded/
server/Old/
server/out/
server/certs/
node_modules/
*.log

# 編集道具
.idea/
.vscode/
Thumbs.db
desktop.ini
