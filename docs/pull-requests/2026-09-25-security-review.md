# 診断の報告(2026-09-25)と、所見を確かめる道具

`claude/loving-heisenberg-ptngxh` → `main`

## 概要

脆弱性カタログ(`security-review-vulnerability-catalog.md`、2026-09-13 付)を物差しにして、
前回の診断(`server/docs/10`)で読んでいなかった範囲・ログイン・個人情報の扱いを調べた報告書を足します。
**所見はすべて、手元に組んだ検証環境で実際に動かして確かめました。** コードは直していません(直すかどうかは別の PR で)。

Android アプリの所見は、非公開の Android のリポジトリ(`docs/security-review-2026-09-25.ipynb`)に分けてあります。

## 入れたもの

| 場所 | 中身 |
|---|---|
| `server/docs/16-security-review-2026-09-25.ipynb` | 報告書。所見 10 件(W-44〜W-53)・直し方の案・検証(§7) |
| `docs/verify-2026-09-25/` | 所見を確かめる道具。`setup.sh`(MariaDB 11.4 のコンテナ・`server/src` の写し・偽の Logto)と `run_all.sh`、その出力 `results/` |

**`server/` の外に置いた理由:** `tools/sync-from-website.ps1` は手元の作業フォルダを `server/` へ /MIR で写すので、
リポジトリにしか無いファイルは次の同期で消えます。**`server/docs/16` は、手元の作業フォルダの `server\docs\` にも置いてください。**

## 所見(重大・高は 0 件)

| 番号 | 深刻度 | 何か |
|---|---|---|
| W-53 | **中** | 公開リポジトリに、実在の教職員の氏名と部屋の組が 4 人分(`check.php`・`Main/app.js:1860`・`docs/plan.md:555`)。**いちばん先に** |
| W-44 | **中** | ランキングの記録が、同じ地点の重複と実在しない地点を数える(1 回で +50、表の行が際限なく増える) |
| W-45 | 低 | 「調べられた語」に、IP を 3 つ用意すれば任意の文言を載せられる |
| W-46 | 低 | 地図のパスワードを替えても、前の解除が残る(教職員氏名が見え続ける) |
| W-47 | 低 | 公開リポジトリ(`server/docs/12`)に運用者の回線の IP。push 前の検査は IP を見ていない |
| W-48 | 低 | アカウントを削除しても、対象者の利用者 ID が監査ログとチャットに残る(ポリシー §8 と食い違う) |
| W-49 | 低 | IP の記録に保存期限が無い。地図の解除に**成功した人の IP も**残る |
| W-50 | 低 | 公開ページで、教職員の特典が ID トークンの期限で消える(不具合) |
| W-51 | 情報 | 教職員の判定に期限切れの ID トークンを使う。そこから出した提案が承認されて地図に入った |
| W-52 | 情報 | 配信の「最新です」の判定が役割を見ない。権限を失った端末に氏名入りの地図が残る |

**報告書には IP アドレス・メールアドレス・氏名の値を書いていません**(場所だけ)。

## 確認したこと

- `setup.sh` → `run_all.sh` を**ゼロから**走らせ、10 件すべて報告書のとおりに振る舞うことを確かめた(出力は `docs/verify-2026-09-25/results/`)
- `composer audit --locked`: 既知の脆弱性 0 件
- 報告書のノートブックは `nbformat` の検証を通る。確認用のセル(🟢)は手元で実行した
- 追加するファイルすべてに、実名・実在の IP が入っていないことを照合した
- **本番には一切触れていない**(SSH・curl・配備なし)。検証環境には nginx・本物の Logto・Soketi・メールは入れていない(§7.1 に違いを書いた)

## 検証で気を付けたこと

表定義を読み込むとき、はじめは取り出し方を誤り、DB のダンプの**データ**(実名を含む)まで手元の使い捨ての DB に入れてしまいました。
気付いてすぐ消し、架空のデータに置き換えています。`setup.sh` は表定義(`schema.sql`、INSERT 0 件)だけを使い、検証の DB は最後に消しました。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01LQAMhxuU1hW2FFYTn9kvpD
