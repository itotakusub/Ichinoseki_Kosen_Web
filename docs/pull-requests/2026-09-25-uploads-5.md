# 診断(2026-09-25)の所見 W-44〜W-53 を直す

`Uploads` → `main`

## 変更点

- W-53 / W-47 実名と運用者の回線の IP を消す。push 前の検査に公開の IPv4・教職員の氏名の照合・-ScanAll を足す
- W-44 / W-45 ランキングは重複と実在しない地点を数えない・語の表に上限・管理画面「ランキングの語」で語を外せる
- W-46 地図のパスワードを替えたら、前の解除は効かない
- W-48 退会した人の ID を監査ログの detail とチャットからも消す
- W-49 監査ログの IP は 90 日・解除の試行は 1 日で消す(ポリシー 8 節も直す)
- W-50 / W-51 期限の切れた ID トークンは取り直してから教職員を判定する
- W-52 配信に中身の段を載せ、段が違えば同じ版でも送り直す

## 変わった場所

| 場所 | ファイル数 |
|---|---|
| `server/docs` | 5 |
| `server/src` | 19 |
| `tools` | 2 |

## 確認したこと

- `php src/scripts/check.php` すべて通過(1449 件)。新しい組 `review-0925` は 56 件(SQLite で実在の絞り込み、偽の OidcCore で ID トークンの取り直し 5 通り、中身の段の判定)
- JavaScript の試験 `scripts/js/run-all.ps1` すべて通過
- `tools/push-github.ps1 -CheckOnly -ScanAll`: 公開の IPv4・教職員の氏名とも 0 件(残る 2 件は前から分かっている誤検知: pusher.min.js と app-map.php)
- **履歴を書き換えた**(2026-09-25、利用者の確認のうえで force push)。`main` と `Uploads` の全 18 コミットで、実名と回線の IP は 0 件。
  元の履歴は手元の `F:\Pull\Old\Ichinoseki_Kosen_Web-before-rewrite-2026-09-25.bundle`。PR #1〜#6 の記録(`refs/pull/*`)には古いコミットが残る
- PR #6(診断の報告と検証の道具)を取り込み、報告書 `server/docs/16` は §5.1(直した一覧)を足したこちらの版を採った
- **本番ではまだ確かめていない。** 配備のあと、`server/docs/16` §5.1 の「本番で確かめること」を 1 つずつ

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## このあとの追加

- PR の説明に確かめたことを書く(診断 2026-09-25 の修正)
