# Website とサーバー構成の最初の取り込み

`Uploads` → `main`

## 概要

高専マップ案内の Website・管理画面・API・Docker Compose 一式・運用の道具を、`server/` に取り込みます。
手元の作業フォルダを正本とし、`tools/sync-from-website.ps1` で**出してよいものだけ**を写しています。

## 入れたもの

| 場所 | 中身 |
|---|---|
| `server/src/` | 公開ページ(地図・経路案内)・管理画面・API・自己検査(`check.php`)・地図の JavaScript |
| `server/docker/` `server/nginx/` `server/compose*.yaml` | コンテナとリバースプロキシの設定 |
| `server/scripts/` | 配備・バックアップ・証明書・更新確認・SSH ログインの知らせと切断/BAN |
| `server/docs/` | 取扱説明書(Notebook。**実行結果は消してある**)と状態の記録 |
| `server/.env.example` | 設定の見本 |
| `tools/` | 控えを作る(`sync-from-website.ps1`)・出す前に検査する(`push-github.ps1`) |

## 入れていないもの(わざと)

- 秘密: `.env`・`src/config/*.local.php`・鍵と証明書
- 利用者のデータ: `src/uploads/`(地図の書き出し。教職員氏名を含みうる)・バックアップ
- 生成物・退避: `src/vendor/`(`composer install` で戻る)・旧サイト `Downloaded/`・`Old/`・ログ

Notebook の本文にあった個人のメールアドレスは `you@example.com` に置き換えています
(手順書で「自分のアドレスに書き換えてから流す」と書いてある箇所)。

## 直近の変更(2026-09-25)

- 地図が錠で閉じているとき、押しても効かない検索・階・表示調整のボタンを下げた
- ルート案内を 1 行の帯「✕ / ◯◯に向かう / 位置の更新」に(下から出るシートを廃止)
- ダウンロード・設定を右から出る引き出しに
- 初回だけの「はじめに」(規約・プライバシー・迷惑メールの注意)
- 屋外で拡大したときの建物名(薄く / 消す / そのまま)・ラベルの重なりほどきは屋外だけ
- SSH ログインを管理者へメールで知らせる(PAM・optional)と、切断・BAN の道具

## 確認したこと

- `check.php` 1310 件・JavaScript 128 件、すべて通過
- 出す前の検査: 出してはいけない名前 0 件。資格情報らしい行の当たり 2 件はどちらも誤検知
  (`admin/vendor/pusher-js/pusher.min.js` の圧縮コードと、`lib/app-map.php` の鍵の**名前** `'app_map_code'`)

## ライセンス

GPL-3.0。同梱のサードパーティ(Leaflet・AdminLTE・composer の依存など)はそれぞれのライセンスに従います。

🤖 Generated with [Claude Code](https://claude.com/claude-code)
