; タイムゾーン。
;
; php:8.4-apache の既定は UTC で、**PHP は TZ 環境変数を見ない**(PHP 5.4 以降、
; date.timezone が空なら問答無用で UTC)。この ini を入れるまで、本番の管理画面は
; タイムライン・チャット履歴・受信箱・カレンダーの「今日」判定まで、すべて 9 時間
; 前の UTC で表示されていた。
;
; 99-local-ca.ini と同じく bind mount で入れているので、イメージの再ビルドは要らない。
date.timezone = Asia/Tokyo
